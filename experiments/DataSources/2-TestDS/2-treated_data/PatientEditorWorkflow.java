package org.imogene.epicam.client.ui.workflow ; public class PatientEditorWorkflow extends EditorWorkflowComposite { interface Driver extends RequestFactoryEditorDriver<PatientProxy, PatientEditor> { }  private EpicamRequestFactory requestFactory ;  private PatientRequest request ;  public PatientProxy current ;  private Driver editorDriver ;  private PatientEditor editor ;  private String initField ;  private boolean showGlassPanel = false ;  * Workflow constructor for the creation of a Patient instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title public PatientEditorWorkflow(EpicamRequestFactory factory, Label titleContainer) { this(factory, titleContainer, null, null) ;  }  * Workflow constructor for the creation of a Patient instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public PatientEditorWorkflow(EpicamRequestFactory factory, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new PatientEditor(factory, true) ;  this.initField = initField ;  }  else editor = new PatientEditor(factory) ;  isNew = true ;  setEditable(true) ;  setTitle(NLS.constants().patient_create_title()) ;  createDriver() ;  createNewPatient() ;  this.setContent(editor) ;  }  * Workflow constructor for the visualization and edition of an existing Patient instance * @param factory the application request factory * @param entityId the id of the Patient instance to be visualized and edited  * @param titleContainer the Label that will display the workflow title  public PatientEditorWorkflow(EpicamRequestFactory factory, String entityId, Label titleContainer) { this(factory, entityId, titleContainer, null, null) ;  }  * Workflow constructor for the visualization and edition of an existing Patient instance * @param factory the application request factory * @param entityId the id of the Patient instance to be visualized and edited  * @param titleContainer the label  * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public PatientEditorWorkflow(EpicamRequestFactory factory, String entityId, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new PatientEditor(factory, true) ;  this.initField = initField ;  }  else editor = new PatientEditor(factory) ;  setModifiable(false) ;  isNew = false ;  setEditable(false) ;  createDriver() ;  fetchPatient(entityId) ;  this.setContent(editor) ;  showGlassPanel = true ;  }  @Override protected void onAttach() { super.onAttach() ;  if (showGlassPanel) { EpicamEntryPoint.GP.showAndAdapt(this) ;  }  }  * Create a new instance of Patient private void createNewPatient() { request = requestFactory.patientRequest() ;  PatientProxy newPatient = request.create(PatientProxy.class) ;  newPatient.setId(ImogKeyGenerator.generateKeyId("PAT")) ;  //create list of CasIndex in editor  newPatient.setCasIndex(new ArrayList<CasIndexProxy>()) ;  //create list of ExamenBiologique in editor  newPatient .setExamensBiologiques(new ArrayList<ExamenBiologiqueProxy>()) ;  //create list of ExamenSerologie in editor  newPatient.setSerologies(new ArrayList<ExamenSerologieProxy>()) ;  current = newPatient ;  editorDriver.edit(current, request) ;  editor.setRequestContextForListEditors(request) ;  editor.computeVisibility(null, true) ;  editor.setEdited(true) ;  }  * Get an existing instance of Patient * @param entityId the id of the PatientProxy to be fetched private void fetchPatient(String entityId) { PatientRequest request = requestFactory.patientRequest() ;  Request<PatientProxy> fetchRequest = request.findById(entityId) ;  fetchRequest.with("centres") ;  fetchRequest.with("lieuxDits") ;  fetchRequest.with("casTuberculose") ;  fetchRequest.with("casTuberculose.patient") ;  fetchRequest.with("casIndex") ;  fetchRequest.with("casIndex.patientLie") ;  fetchRequest.with("examensBiologiques") ;  fetchRequest.with("serologies") ;  fetchRequest.to(new Receiver<PatientProxy>() { @Override public void onSuccess(PatientProxy entity) { viewPatient(entity) ;  }  } ).fire() ;  }  * Display the current instance of Patient in editor * @param entity the PatientProxy to be displayed private void viewPatient(PatientProxy entity) { setTitle(NLS.constants().patient_name() + ": " + EpicamRenderer.get().getDisplayValue(entity)) ;  setMetaData((ImogBeanProxy) entity) ;  request = requestFactory.patientRequest() ;  current = request.edit(entity) ;  List<CasIndexProxy> casIndex = current.getCasIndex() ;  if (casIndex != null && casIndex.size() > 0) { for (CasIndexProxy item : casIndex) { }  }  List<ExamenBiologiqueProxy> examensBiologiques = current .getExamensBiologiques() ;  if (examensBiologiques != null && examensBiologiques.size() > 0) { for (ExamenBiologiqueProxy item : examensBiologiques) { }  }  List<ExamenSerologieProxy> serologies = current.getSerologies() ;  if (serologies != null && serologies.size() > 0) { for (ExamenSerologieProxy item : serologies) { }  }  editor.setEditedValue(current) ;  editor.setRequestContextForListEditors(request) ;  editorDriver.edit(current, request) ;  editor.setEdited(false) ;  editor.computeVisibility(null, true) ;  if (AccessManager.canEditPatient()) setModifiable(true) ;  showGlassPanel = false ;  EpicamEntryPoint.GP.hide() ;  }  * Edit the current instance of Patient in editor @Override protected void edit() { editor.setEdited(true) ;  }  * Initialize the editor driver private void createDriver() { if (editorDriver == null) { editorDriver = GWT.create(Driver.class) ;  editorDriver.initialize(requestFactory, editor) ;  }  }  * Persist the current instance of Patient @Override protected void save() { editor.validateFields() ;  editorDriver.flush() ;  // Check for errors on the client side if (editorDriver.hasErrors()) { //Window.alert("Patient form not validated locally") ;  return ;  }  Request<Void> saveRequest = request.save(current, isNew) ;  saveRequest.to(new Receiver<Void>() { @Override public void onSuccess(Void response) { requestFactory.getEventBus().fireEvent( new SavePatientEvent(current, initField)) ;  closeForm() ;  }  @Override public void onConstraintViolation(Set<ConstraintViolation<?>> errors) { //Window.alert("Patient form not validated on server") ;  //TODO manage errors on client side when made available by GWT  if (errors != null && errors.size() > 0) { // convert ConstraintViolation to get localized messages EpicamRenderer renderer = EpicamRenderer.get() ;  Set<ConstraintViolation<?>> imogErrors = new HashSet<ConstraintViolation<?>>() ;  for (ConstraintViolation<?> error : errors) { ImogConstraintViolation violation = new ImogConstraintViolation() ;  violation.setLeafBean((BaseProxy) error.getLeafBean()) ;  violation.setPropertyPath(error.getPropertyPath()) ;  violation.setRootBean((BaseProxy) error.getRootBean()) ;  violation.setMessage(renderer.getI18nErrorMessage(error .getMessage())) ;  imogErrors.add(violation) ;  }  editorDriver.setConstraintViolations(imogErrors) ;  }  }  @Override public void onFailure(ServerFailure error) { Window.alert("Error updating the Patient") ;  super.onFailure(error) ;  }  } ) ;  request.fire() ;  }  @Override protected void cancel() { if (parent != null) parent.hide() ;  else { if (isNew) requestFactory.getEventBus().fireEvent(closeEvent) ;  else requestFactory.getEventBus().fireEvent( new ViewPatientEvent(current.getId(), closeEvent)) ;  }  }  @Override protected void returnToList() { requestFactory.getEventBus().fireEvent(new ListPatientEvent()) ;  } }