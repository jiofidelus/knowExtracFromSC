package org.imogene.epicam.client.ui.workflow ; public class RegionEditorWorkflow extends EditorWorkflowComposite { interface Driver extends RequestFactoryEditorDriver<RegionProxy, RegionEditor> { }  private EpicamRequestFactory requestFactory ;  private RegionRequest request ;  public RegionProxy current ;  private Driver editorDriver ;  private RegionEditor editor ;  private String initField ;  private boolean showGlassPanel = false ;  * Workflow constructor for the creation of a Region instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title public RegionEditorWorkflow(EpicamRequestFactory factory, Label titleContainer) { this(factory, titleContainer, null, null) ;  }  * Workflow constructor for the creation of a Region instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public RegionEditorWorkflow(EpicamRequestFactory factory, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new RegionEditor(factory, true) ;  this.initField = initField ;  }  else editor = new RegionEditor(factory) ;  isNew = true ;  setEditable(true) ;  setTitle(NLS.constants().region_create_title()) ;  createDriver() ;  createNewRegion() ;  this.setContent(editor) ;  }  * Workflow constructor for the visualization and edition of an existing Region instance * @param factory the application request factory * @param entityId the id of the Region instance to be visualized and edited  * @param titleContainer the Label that will display the workflow title  public RegionEditorWorkflow(EpicamRequestFactory factory, String entityId, Label titleContainer) { this(factory, entityId, titleContainer, null, null) ;  }  * Workflow constructor for the visualization and edition of an existing Region instance * @param factory the application request factory * @param entityId the id of the Region instance to be visualized and edited  * @param titleContainer the label  * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public RegionEditorWorkflow(EpicamRequestFactory factory, String entityId, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new RegionEditor(factory, true) ;  this.initField = initField ;  }  else editor = new RegionEditor(factory) ;  setModifiable(false) ;  isNew = false ;  setEditable(false) ;  createDriver() ;  fetchRegion(entityId) ;  this.setContent(editor) ;  showGlassPanel = true ;  }  @Override protected void onAttach() { super.onAttach() ;  if (showGlassPanel) { EpicamEntryPoint.GP.showAndAdapt(this) ;  }  }  * Create a new instance of Region private void createNewRegion() { request = requestFactory.regionRequest() ;  RegionProxy newRegion = request.create(RegionProxy.class) ;  newRegion.setId(ImogKeyGenerator.generateKeyId("REG")) ;  LocalizedTextProxy newNom = request.create(LocalizedTextProxy.class) ;  newRegion.setNom(newNom) ;  LocalizedTextProxy newDescription = request .create(LocalizedTextProxy.class) ;  newRegion.setDescription(newDescription) ;  GeoFieldProxy newCoordonnees = request.create(GeoFieldProxy.class) ;  newRegion.setCoordonnees(newCoordonnees) ;  current = newRegion ;  editorDriver.edit(current, request) ;  editor.setRequestContextForListEditors(request) ;  editor.computeVisibility(null, true) ;  editor.setEdited(true) ;  }  * Get an existing instance of Region * @param entityId the id of the RegionProxy to be fetched private void fetchRegion(String entityId) { RegionRequest request = requestFactory.regionRequest() ;  Request<RegionProxy> fetchRequest = request.findById(entityId) ;  fetchRequest.with("nom") ;  fetchRequest.with("description") ;  fetchRequest.with("districtSantes") ;  fetchRequest.with("districtSantes.nom") ;  fetchRequest.with("coordonnees") ;  fetchRequest.to(new Receiver<RegionProxy>() { @Override public void onSuccess(RegionProxy entity) { viewRegion(entity) ;  }  } ).fire() ;  }  * Display the current instance of Region in editor * @param entity the RegionProxy to be displayed private void viewRegion(RegionProxy entity) { setTitle(NLS.constants().region_name() + ": " + EpicamRenderer.get().getDisplayValue(entity)) ;  setMetaData((ImogBeanProxy) entity) ;  request = requestFactory.regionRequest() ;  current = request.edit(entity) ;  if (current.getNom() == null) { LocalizedTextProxy newNom = request .create(LocalizedTextProxy.class) ;  current.setNom(newNom) ;  }  if (current.getDescription() == null) { LocalizedTextProxy newDescription = request .create(LocalizedTextProxy.class) ;  current.setDescription(newDescription) ;  }  if (current.getCoordonnees() == null) { GeoFieldProxy newCoordonnees = request.create(GeoFieldProxy.class) ;  current.setCoordonnees(newCoordonnees) ;  }  editor.setEditedValue(current) ;  editor.setRequestContextForListEditors(request) ;  editorDriver.edit(current, request) ;  editor.setEdited(false) ;  editor.computeVisibility(null, true) ;  if (AccessManager.canEditRegion()) setModifiable(true) ;  showGlassPanel = false ;  EpicamEntryPoint.GP.hide() ;  }  * Edit the current instance of Region in editor @Override protected void edit() { editor.setEdited(true) ;  }  * Initialize the editor driver private void createDriver() { if (editorDriver == null) { editorDriver = GWT.create(Driver.class) ;  editorDriver.initialize(requestFactory, editor) ;  }  }  * Persist the current instance of Region @Override protected void save() { editor.validateFields() ;  editorDriver.flush() ;  // Check for errors on the client side if (editorDriver.hasErrors()) { //Window.alert("Region form not validated locally") ;  return ;  }  Request<Void> saveRequest = request.save(current, isNew) ;  saveRequest.to(new Receiver<Void>() { @Override public void onSuccess(Void response) { requestFactory.getEventBus().fireEvent( new SaveRegionEvent(current, initField)) ;  closeForm() ;  }  @Override public void onConstraintViolation(Set<ConstraintViolation<?>> errors) { //Window.alert("Region form not validated on server") ;  //TODO manage errors on client side when made available by GWT  if (errors != null && errors.size() > 0) { // convert ConstraintViolation to get localized messages EpicamRenderer renderer = EpicamRenderer.get() ;  Set<ConstraintViolation<?>> imogErrors = new HashSet<ConstraintViolation<?>>() ;  for (ConstraintViolation<?> error : errors) { ImogConstraintViolation violation = new ImogConstraintViolation() ;  violation.setLeafBean((BaseProxy) error.getLeafBean()) ;  violation.setPropertyPath(error.getPropertyPath()) ;  violation.setRootBean((BaseProxy) error.getRootBean()) ;  violation.setMessage(renderer.getI18nErrorMessage(error .getMessage())) ;  imogErrors.add(violation) ;  }  editorDriver.setConstraintViolations(imogErrors) ;  }  }  @Override public void onFailure(ServerFailure error) { Window.alert("Error updating the Region") ;  super.onFailure(error) ;  }  } ) ;  request.fire() ;  }  @Override protected void cancel() { if (parent != null) parent.hide() ;  else { if (isNew) requestFactory.getEventBus().fireEvent(closeEvent) ;  else requestFactory.getEventBus().fireEvent( new ViewRegionEvent(current.getId(), closeEvent)) ;  }  }  @Override protected void returnToList() { requestFactory.getEventBus().fireEvent(new ListRegionEvent()) ;  } }