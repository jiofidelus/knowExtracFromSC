package org.imogene.admin.client.ui.workflow ; public class CardEntityEditorWorkflow extends EditorWorkflowComposite { interface Driver extends RequestFactoryEditorDriver<CardEntityProxy, CardEntityEditor> { }  private AdminRequestFactory requestFactory ;  private ImogBeanRenderer renderer ;  private CardEntityRequest request ;  public CardEntityProxy current ;  private Driver editorDriver ;  private CardEntityEditor editor ;  private String initField ;  * Workflow constructor for the creation of a CardEntity instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title public CardEntityEditorWorkflow(AdminRequestFactory factory, Label titleContainer) { this(factory, titleContainer, null, null) ;  }  * Workflow constructor for the creation of a CardEntity instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public CardEntityEditorWorkflow(AdminRequestFactory factory, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new CardEntityEditor(factory, true) ;  this.initField = initField ;  }  else editor = new CardEntityEditor(factory) ;  isNew = true ;  setEditable(true) ;  setTitle(AdminNLS.constants().cardEntity_create_title()) ;  createDriver() ;  createNewCardEntity() ;  this.setContent(editor) ;  }  * Workflow constructor for the visualization and edition of an existing CardEntity instance * @param factory the application request factory * @param entityId the id of the CardEntity instance to be visualized and edited * @param titleContainer the Label that will display the workflow title public CardEntityEditorWorkflow(AdminRequestFactory factory, String entityId, Label titleContainer, ImogBeanRenderer renderer) { this(factory, entityId, titleContainer, null, null, renderer) ;  }  * Workflow constructor for the visualization and edition of an existing CardEntity instance * @param factory the application request factory * @param entityId the id of the CardEntity instance to be visualized and edited * @param titleContainer the label * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public CardEntityEditorWorkflow(AdminRequestFactory factory, String entityId, Label titleContainer, RelationPopupPanel parent, String initField, ImogBeanRenderer renderer) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  this.renderer = renderer ;  if (parent != null) { editor = new CardEntityEditor(factory, true) ;  this.initField = initField ;  }  else editor = new CardEntityEditor(factory) ;  setModifiable(false) ;  isNew = false ;  setEditable(false) ;  createDriver() ;  fetchCardEntity(entityId) ;  this.setContent(editor) ;  }  @Override protected void onAttach() { super.onAttach() ;  }  * Create a new instance of CardEntity private void createNewCardEntity() { request = requestFactory.cardEntityRequest() ;  CardEntityProxy newCardEntity = request.create(CardEntityProxy.class) ;  newCardEntity.setId(ImogKeyGenerator.generateKeyId("SYNC_ENT")) ;  current = newCardEntity ;  editorDriver.edit(current, request) ;  editor.setRequestContextForListEditors(request) ;  editor.computeVisibility(null, true) ;  editor.setEdited(true) ;  }  * Get an existing instance of CardEntity * @param entityId the id of the CardEntityProxy to be fetched private void fetchCardEntity(String entityId) { CardEntityRequest request = requestFactory.cardEntityRequest() ;  Request<CardEntityProxy> fetchRequest = request.findById(entityId) ;  fetchRequest.to(new Receiver<CardEntityProxy>() { @Override public void onSuccess(CardEntityProxy entity) { viewCardEntity(entity) ;  }  } ).fire() ;  }  * Display the current instance of CardEntity in editor * @param entity the CardEntityProxy to be displayed private void viewCardEntity(CardEntityProxy entity) { setTitle(AdminNLS.constants().cardEntity_name() + ": " + renderer.getDisplayValue(entity)) ;  setMetaData((ImogBeanProxy) entity) ;  request = requestFactory.cardEntityRequest() ;  current = request.edit(entity) ;  editor.setEditedValue(current) ;  editor.setRequestContextForListEditors(request) ;  editorDriver.edit(current, request) ;  editor.setEdited(false) ;  editor.computeVisibility(null, true) ;  }  * Edit the current instance of CardEntity in editor @Override protected void edit() { editor.setEdited(true) ;  }  * Initialize the editor driver private void createDriver() { if (editorDriver == null) { editorDriver = GWT.create(Driver.class) ;  editorDriver.initialize(requestFactory, editor) ;  }  }  * Persist the current instance of CardEntity @Override protected void save() { editor.validateFields() ;  editorDriver.flush() ;  // Check for errors on the client side if (editorDriver.hasErrors()) { // Window.alert("CardEntity form not validated locally") ;  return ;  }  Request<Void> saveRequest = request.save(current, isNew) ;  saveRequest.to(new Receiver<Void>() { @Override public void onSuccess(Void response) { requestFactory.getEventBus().fireEvent(new SaveCardEntityEvent(current, initField)) ;  closeForm() ;  }  @Override public void onConstraintViolation(Set<ConstraintViolation<?>> errors) { // Window.alert("CardEntity form not validated on server") ;  // TODO manage errors on client side when made available by GWT if (errors != null && errors.size() > 0) { // convert ConstraintViolation to get localized messages AdminRenderer renderer = AdminRenderer.get() ;  Set<ConstraintViolation<?>> imogErrors = new HashSet<ConstraintViolation<?>>() ;  for (ConstraintViolation<?> error : errors) { ImogConstraintViolation violation = new ImogConstraintViolation() ;  violation.setLeafBean((BaseProxy) error.getLeafBean()) ;  violation.setPropertyPath(error.getPropertyPath()) ;  violation.setRootBean((BaseProxy) error.getRootBean()) ;  violation.setMessage(renderer.getI18nErrorMessage(error.getMessage())) ;  imogErrors.add(violation) ;  }  editorDriver.setConstraintViolations(imogErrors) ;  }  }  @Override public void onFailure(ServerFailure error) { Window.alert("Error updating the CardEntity") ;  super.onFailure(error) ;  }  } ) ;  request.fire() ;  }  @Override protected void cancel() { if (parent != null) parent.hide() ;  else { if (isNew) requestFactory.getEventBus().fireEvent(closeEvent) ;  else requestFactory.getEventBus().fireEvent(new ViewCardEntityEvent(current.getId(), closeEvent)) ;  }  }  @Override protected void returnToList() { requestFactory.getEventBus().fireEvent(new ListCardEntityEvent()) ;  } }