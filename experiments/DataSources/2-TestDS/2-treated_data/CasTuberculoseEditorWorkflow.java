package org.imogene.epicam.client.ui.workflow ; public class CasTuberculoseEditorWorkflow extends EditorWorkflowComposite { interface Driver extends RequestFactoryEditorDriver<CasTuberculoseProxy, CasTuberculoseEditor> { }  private EpicamRequestFactory requestFactory ;  private CasTuberculoseRequest request ;  public CasTuberculoseProxy current ;  private Driver editorDriver ;  private CasTuberculoseEditor editor ;  private String initField ;  private boolean showGlassPanel = false ;  * Workflow constructor for the creation of a CasTuberculose instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title public CasTuberculoseEditorWorkflow(EpicamRequestFactory factory, Label titleContainer) { this(factory, titleContainer, null, null) ;  }  * Workflow constructor for the creation of a CasTuberculose instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public CasTuberculoseEditorWorkflow(EpicamRequestFactory factory, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new CasTuberculoseEditor(factory, true) ;  this.initField = initField ;  }  else editor = new CasTuberculoseEditor(factory) ;  isNew = true ;  setEditable(true) ;  setTitle(NLS.constants().casTuberculose_create_title()) ;  createDriver() ;  createNewCasTuberculose() ;  this.setContent(editor) ;  }  * Workflow constructor for the visualization and edition of an existing CasTuberculose instance * @param factory the application request factory * @param entityId the id of the CasTuberculose instance to be visualized and edited  * @param titleContainer the Label that will display the workflow title  public CasTuberculoseEditorWorkflow(EpicamRequestFactory factory, String entityId, Label titleContainer) { this(factory, entityId, titleContainer, null, null) ;  }  * Workflow constructor for the visualization and edition of an existing CasTuberculose instance * @param factory the application request factory * @param entityId the id of the CasTuberculose instance to be visualized and edited  * @param titleContainer the label  * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public CasTuberculoseEditorWorkflow(EpicamRequestFactory factory, String entityId, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new CasTuberculoseEditor(factory, true) ;  this.initField = initField ;  }  else editor = new CasTuberculoseEditor(factory) ;  setModifiable(false) ;  isNew = false ;  setEditable(false) ;  createDriver() ;  fetchCasTuberculose(entityId) ;  this.setContent(editor) ;  showGlassPanel = true ;  }  @Override protected void onAttach() { super.onAttach() ;  if (showGlassPanel) { EpicamEntryPoint.GP.showAndAdapt(this) ;  }  }  * Create a new instance of CasTuberculose private void createNewCasTuberculose() { request = requestFactory.casTuberculoseRequest() ;  CasTuberculoseProxy newCasTuberculose = request .create(CasTuberculoseProxy.class) ;  newCasTuberculose.setId(ImogKeyGenerator.generateKeyId("TBCASE")) ;  //create list of PriseMedicamenteuse in editor  newCasTuberculose .setPriseMedicamenteusePhaseInitiale(new ArrayList<PriseMedicamenteuseProxy>()) ;  //create list of PriseMedicamenteuse in editor  newCasTuberculose .setPriseMedicamenteusePhaseContinuation(new ArrayList<PriseMedicamenteuseProxy>()) ;  //create list of RendezVous in editor  newCasTuberculose.setRendezVous(new ArrayList<RendezVousProxy>()) ;  current = newCasTuberculose ;  editorDriver.edit(current, request) ;  editor.setRequestContextForListEditors(request) ;  editor.computeVisibility(null, true) ;  editor.setEdited(true) ;  }  * Get an existing instance of CasTuberculose * @param entityId the id of the CasTuberculoseProxy to be fetched private void fetchCasTuberculose(String entityId) { CasTuberculoseRequest request = requestFactory.casTuberculoseRequest() ;  Request<CasTuberculoseProxy> fetchRequest = request.findById(entityId) ;  fetchRequest.with("patient") ;  fetchRequest.with("examensMiscrocopies") ;  fetchRequest.with("examensATB") ;  fetchRequest.with("regimePhaseInitiale") ;  fetchRequest.with("regimePhaseContinuation") ;  fetchRequest.with("priseMedicamenteusePhaseInitiale") ;  fetchRequest.with("priseMedicamenteusePhaseContinuation") ;  fetchRequest.with("rendezVous") ;  fetchRequest.to(new Receiver<CasTuberculoseProxy>() { @Override public void onSuccess(CasTuberculoseProxy entity) { viewCasTuberculose(entity) ;  }  } ).fire() ;  }  * Display the current instance of CasTuberculose in editor * @param entity the CasTuberculoseProxy to be displayed private void viewCasTuberculose(CasTuberculoseProxy entity) { setTitle(NLS.constants().casTuberculose_name() + ": " + EpicamRenderer.get().getDisplayValue(entity)) ;  setMetaData((ImogBeanProxy) entity) ;  request = requestFactory.casTuberculoseRequest() ;  current = request.edit(entity) ;  List<PriseMedicamenteuseProxy> priseMedicamenteusePhaseInitiale = current .getPriseMedicamenteusePhaseInitiale() ;  if (priseMedicamenteusePhaseInitiale != null && priseMedicamenteusePhaseInitiale.size() > 0) { for (PriseMedicamenteuseProxy item : priseMedicamenteusePhaseInitiale) { }  }  List<PriseMedicamenteuseProxy> priseMedicamenteusePhaseContinuation = current .getPriseMedicamenteusePhaseContinuation() ;  if (priseMedicamenteusePhaseContinuation != null && priseMedicamenteusePhaseContinuation.size() > 0) { for (PriseMedicamenteuseProxy item : priseMedicamenteusePhaseContinuation) { }  }  List<RendezVousProxy> rendezVous = current.getRendezVous() ;  if (rendezVous != null && rendezVous.size() > 0) { for (RendezVousProxy item : rendezVous) { }  }  editor.setEditedValue(current) ;  editor.setRequestContextForListEditors(request) ;  editorDriver.edit(current, request) ;  editor.setEdited(false) ;  editor.computeVisibility(null, true) ;  if (AccessManager.canEditCasTuberculose()) setModifiable(true) ;  showGlassPanel = false ;  EpicamEntryPoint.GP.hide() ;  }  * Edit the current instance of CasTuberculose in editor @Override protected void edit() { editor.setEdited(true) ;  }  * Initialize the editor driver private void createDriver() { if (editorDriver == null) { editorDriver = GWT.create(Driver.class) ;  editorDriver.initialize(requestFactory, editor) ;  }  }  * Persist the current instance of CasTuberculose @Override protected void save() { editor.validateFields() ;  editorDriver.flush() ;  // Check for errors on the client side if (editorDriver.hasErrors()) { //Window.alert("CasTuberculose form not validated locally") ;  return ;  }  Request<Void> saveRequest = request.save(current, isNew) ;  saveRequest.to(new Receiver<Void>() { @Override public void onSuccess(Void response) { requestFactory.getEventBus().fireEvent( new SaveCasTuberculoseEvent(current, initField)) ;  closeForm() ;  }  @Override public void onConstraintViolation(Set<ConstraintViolation<?>> errors) { //Window.alert("CasTuberculose form not validated on server") ;  //TODO manage errors on client side when made available by GWT  if (errors != null && errors.size() > 0) { // convert ConstraintViolation to get localized messages EpicamRenderer renderer = EpicamRenderer.get() ;  Set<ConstraintViolation<?>> imogErrors = new HashSet<ConstraintViolation<?>>() ;  for (ConstraintViolation<?> error : errors) { ImogConstraintViolation violation = new ImogConstraintViolation() ;  violation.setLeafBean((BaseProxy) error.getLeafBean()) ;  violation.setPropertyPath(error.getPropertyPath()) ;  violation.setRootBean((BaseProxy) error.getRootBean()) ;  violation.setMessage(renderer.getI18nErrorMessage(error .getMessage())) ;  imogErrors.add(violation) ;  }  editorDriver.setConstraintViolations(imogErrors) ;  }  }  @Override public void onFailure(ServerFailure error) { Window.alert("Error updating the CasTuberculose") ;  super.onFailure(error) ;  }  } ) ;  request.fire() ;  }  @Override protected void cancel() { if (parent != null) parent.hide() ;  else { if (isNew) requestFactory.getEventBus().fireEvent(closeEvent) ;  else requestFactory.getEventBus() .fireEvent( new ViewCasTuberculoseEvent(current.getId(), closeEvent)) ;  }  }  @Override protected void returnToList() { requestFactory.getEventBus().fireEvent(new ListCasTuberculoseEvent()) ;  }  * Setter to inject a Patient value * @param value the value to be injected * @param isLocked true if relation field shall be locked (non editable) public void setPatient(PatientProxy value, boolean isLocked) { editor.setPatient(value, isLocked) ;  }  * Setter to inject a Regime value * @param value the value to be injected * @param isLocked true if relation field shall be locked (non editable) public void setRegimePhaseInitiale(RegimeProxy value, boolean isLocked) { editor.setRegimePhaseInitiale(value, isLocked) ;  }  * Setter to inject a Regime value * @param value the value to be injected * @param isLocked true if relation field shall be locked (non editable) public void setRegimePhaseContinuation(RegimeProxy value, boolean isLocked) { editor.setRegimePhaseContinuation(value, isLocked) ;  } }