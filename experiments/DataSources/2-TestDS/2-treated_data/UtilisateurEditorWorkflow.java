package org.imogene.epicam.client.ui.workflow ; public class UtilisateurEditorWorkflow extends EditorWorkflowComposite { interface Driver extends RequestFactoryEditorDriver<UtilisateurProxy, UtilisateurEditor> { }  private EpicamRequestFactory requestFactory ;  private UtilisateurRequest request ;  public UtilisateurProxy current ;  private Driver editorDriver ;  private UtilisateurEditor editor ;  private String initField ;  private boolean showGlassPanel = false ;  * Workflow constructor for the creation of a Utilisateur instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title public UtilisateurEditorWorkflow(EpicamRequestFactory factory, Label titleContainer) { this(factory, titleContainer, null, null) ;  }  * Workflow constructor for the creation of a Utilisateur instance * @param factory the application request factory * @param titleContainer the Label that will display the workflow title * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public UtilisateurEditorWorkflow(EpicamRequestFactory factory, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new UtilisateurEditor(factory, true) ;  this.initField = initField ;  }  else editor = new UtilisateurEditor(factory) ;  isNew = true ;  setEditable(true) ;  setTitle(NLS.constants().utilisateur_create_title()) ;  createDriver() ;  createNewUtilisateur() ;  this.setContent(editor) ;  }  * Workflow constructor for the visualization and edition of an existing Utilisateur instance * @param factory the application request factory * @param entityId the id of the Utilisateur instance to be visualized and edited  * @param titleContainer the Label that will display the workflow title  public UtilisateurEditorWorkflow(EpicamRequestFactory factory, String entityId, Label titleContainer) { this(factory, entityId, titleContainer, null, null) ;  }  * Workflow constructor for the visualization and edition of an existing Utilisateur instance * @param factory the application request factory * @param entityId the id of the Utilisateur instance to be visualized and edited  * @param titleContainer the label  * @param parent the parent RelationPopupPanel when the workflow is opened from a relation field * @param initField the name of the field that initiated the opening of the workflow when the workflow is opened from a relation field public UtilisateurEditorWorkflow(EpicamRequestFactory factory, String entityId, Label titleContainer, RelationPopupPanel parent, String initField) { super(factory.getEventBus(), titleContainer, parent) ;  requestFactory = factory ;  if (parent != null) { editor = new UtilisateurEditor(factory, true) ;  this.initField = initField ;  }  else editor = new UtilisateurEditor(factory) ;  setModifiable(false) ;  isNew = false ;  setEditable(false) ;  createDriver() ;  fetchUtilisateur(entityId) ;  this.setContent(editor) ;  showGlassPanel = true ;  }  @Override protected void onAttach() { super.onAttach() ;  if (showGlassPanel) { EpicamEntryPoint.GP.showAndAdapt(this) ;  }  }  * Create a new instance of Utilisateur private void createNewUtilisateur() { request = requestFactory.utilisateurRequest() ;  UtilisateurProxy newUtilisateur = request .create(UtilisateurProxy.class) ;  newUtilisateur.setId(ImogKeyGenerator.generateKeyId("USR")) ;  current = newUtilisateur ;  editorDriver.edit(current, request) ;  editor.setRequestContextForListEditors(request) ;  editor.computeVisibility(null, true) ;  editor.setEdited(true) ;  }  * Get an existing instance of Utilisateur * @param entityId the id of the UtilisateurProxy to be fetched private void fetchUtilisateur(String entityId) { UtilisateurRequest request = requestFactory.utilisateurRequest() ;  Request<UtilisateurProxy> fetchRequest = request.findById(entityId) ;  fetchRequest.with("profiles") ;  fetchRequest.with("synchronizables") ;  fetchRequest.to(new Receiver<UtilisateurProxy>() { @Override public void onSuccess(UtilisateurProxy entity) { viewUtilisateur(entity) ;  }  } ).fire() ;  }  * Display the current instance of Utilisateur in editor * @param entity the UtilisateurProxy to be displayed private void viewUtilisateur(UtilisateurProxy entity) { setTitle(NLS.constants().utilisateur_name() + ": " + EpicamRenderer.get().getDisplayValue(entity)) ;  setMetaData((ImogBeanProxy) entity) ;  request = requestFactory.utilisateurRequest() ;  current = request.edit(entity) ;  editor.setEditedValue(current) ;  editor.setRequestContextForListEditors(request) ;  editorDriver.edit(current, request) ;  editor.setEdited(false) ;  editor.computeVisibility(null, true) ;  editor.updateIdLink(current.getId()) ;  editor.setCurrentLogin() ;  if (AccessManager.canEditUtilisateur()) setModifiable(true) ;  showGlassPanel = false ;  EpicamEntryPoint.GP.hide() ;  }  * Edit the current instance of Utilisateur in editor @Override protected void edit() { editor.setEdited(true) ;  }  * Initialize the editor driver private void createDriver() { if (editorDriver == null) { editorDriver = GWT.create(Driver.class) ;  editorDriver.initialize(requestFactory, editor) ;  }  }  * Persist the current instance of Utilisateur @Override protected void save() { editor.validateFields() ;  boolean passwordValid = editor.validatePasword() ;  editor.validateLoginWithPassword() ;  editorDriver.flush() ;  // Check for errors on the client side if (editorDriver.hasErrors()) { //Window.alert("Utilisateur form not validated locally") ;  return ;  }  Request<Void> saveRequest = null ;  if (passwordValid && editor.passwordChanged()) saveRequest = request.save(current, isNew, true) ;  else saveRequest = request.save(current, isNew, false) ;  saveRequest.to(new Receiver<Void>() { @Override public void onSuccess(Void response) { requestFactory.getEventBus().fireEvent( new SaveUtilisateurEvent(current, initField)) ;  closeForm() ;  }  @Override public void onConstraintViolation(Set<ConstraintViolation<?>> errors) { //Window.alert("Utilisateur form not validated on server") ;  //TODO manage errors on client side when made available by GWT  if (errors != null && errors.size() > 0) { // convert ConstraintViolation to get localized messages EpicamRenderer renderer = EpicamRenderer.get() ;  Set<ConstraintViolation<?>> imogErrors = new HashSet<ConstraintViolation<?>>() ;  for (ConstraintViolation<?> error : errors) { ImogConstraintViolation violation = new ImogConstraintViolation() ;  violation.setLeafBean((BaseProxy) error.getLeafBean()) ;  violation.setPropertyPath(error.getPropertyPath()) ;  violation.setRootBean((BaseProxy) error.getRootBean()) ;  violation.setMessage(renderer.getI18nErrorMessage(error .getMessage())) ;  imogErrors.add(violation) ;  }  editorDriver.setConstraintViolations(imogErrors) ;  }  }  @Override public void onFailure(ServerFailure error) { Window.alert("Error updating the Utilisateur") ;  super.onFailure(error) ;  }  } ) ;  request.fire() ;  }  @Override protected void cancel() { if (parent != null) parent.hide() ;  else { if (isNew) requestFactory.getEventBus().fireEvent(closeEvent) ;  else requestFactory.getEventBus().fireEvent( new ViewUtilisateurEvent(current.getId(), closeEvent)) ;  }  }  @Override protected void returnToList() { requestFactory.getEventBus().fireEvent(new ListUtilisateurEvent()) ;  } }